package one.digitalinnovation.collections

fun main() {

    // Array dinâmico, com tamanho inferido de acordo com os valores dentro dele, sem tamanho pré-definido.
    val values = intArrayOf(5, 8, 0, 2, 6, 15)

    for (valor in values){
        println(valor)
    }

}